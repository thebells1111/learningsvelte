<!-- <script context="module">
  export function preload(req) {
    //console.log(req.path.substring(1, req.path.length));
    this.redirect(301, `${req.path.substring(1, req.path.length)}/intro`);
  }
</script> -->
